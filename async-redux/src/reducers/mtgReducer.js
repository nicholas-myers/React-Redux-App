const initialState = {
    isFetching: false,
    data: [],
    error: ""
}

export const mtgReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}